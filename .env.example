# Copy to .env (gitignored) and fill in. The variable name must match
# `api_key_env` in ~/.config/aigentic/config.toml. The key is read only from
# the environment, never from the config file.
TENSORX_API_KEY=
